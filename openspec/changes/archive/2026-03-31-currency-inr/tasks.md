## 1. UI Symbol Updates

- [x] 1.1 Update `AddScheduleForm.tsx` — change `Amount ($)` label to `Amount (₹)`.
- [x] 1.2 Update `projects/[id]/page.tsx` — change `` `$${s.amount}` `` to `` `₹${s.amount}` `` in the schedule table.
- [x] 1.3 Update `page.tsx` (dashboard) — replace all three `$` amount prefixes with `₹` across the overdue, this week, and upcoming card sections.

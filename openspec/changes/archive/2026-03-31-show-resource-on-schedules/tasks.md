## 1. Update Server Actions

- [x] 1.1 Update `getCategorizedSchedules` in `src/lib/actions.ts` to include the `resource` relation.
- [x] 1.2 Update `getProjectById` in `src/lib/actions.ts` to include the `resource` relation in the schedules list.
- [x] 1.3 Update `getResourcesWithStats` in `src/lib/actions.ts` to include the `project` relation.

## 2. Dashboard UI

- [x] 2.1 Update `DashboardPage` in `src/app/page.tsx` to display the resource name on كل schedule card.

## 3. Project Detail UI

- [x] 3.1 Update `ProjectDetailPage` in `src/app/projects/[id]/page.tsx` to display the resource name in the schedule table.
